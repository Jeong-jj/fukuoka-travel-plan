# Fukuoka Trip Agent Guide

## 문서 역할

이 파일은 저장소에서 작업하는 코딩 에이전트의 실행 규칙이다. 여행자의 의도와 결정 배경은 `PROJECT_HANDOFF.md`를 먼저 읽는다. 일정, 가격, 매장, 예산처럼 바뀔 수 있는 값은 문서에 복제하지 않고 최신 코드에서 확인한다.

정보가 충돌하면 다음 순서로 판단한다.

1. 사용자의 최신 직접 지시
2. 최신 저장소 데이터와 코드
3. `PROJECT_HANDOFF.md`의 의도와 결정 배경
4. 과거 조사 자료

## 프로젝트 기본 정보

- React 19, TypeScript, Vite, Tailwind CSS 4 기반의 정적 SPA다.
- 패키지 매니저는 pnpm이다.
- 주요 화면은 `src/App.tsx`, 전역 스타일은 `src/index.css`에서 관리한다.
- 공통 UI는 `components/ui`, 훅은 `hooks`, 유틸리티는 `lib`에 둔다.
- 개발은 `pnpm dev`, 빌드는 `pnpm build`로 검증한다.
- `main` 브랜치에 푸시하면 GitHub Actions가 GitHub Pages에 배포한다.
- 빌드 산출물인 `dist`를 직접 수정하지 않는다.

## 작업 전 확인

1. `PROJECT_HANDOFF.md`를 읽고 변경 대상과 관련된 사용자 의도를 확인한다.
2. `git status --short --branch`로 기존 변경 사항을 확인하고 사용자 작업을 덮어쓰지 않는다.
3. 일정, 쇼핑, 예산, 장소 데이터는 현재 코드에서 다시 확인한다.
4. 가격, 영업시간, 교통편, 행사 시간처럼 변하는 정보를 조사할 때는 최신 공식 출처를 우선한다.

## 콘텐츠 원칙

- 한 영역을 개선하면서 다른 영역의 기존 데이터를 삭제하거나 축약하지 않는다.
- 리팩터링 전후에 일정, 교통, 매장, 구매 품목, 가격, 예산, 비교 링크, Plan B가 보존됐는지 확인한다.
- 확인된 사실, 예상값, 미확정 정보를 명확히 구분한다. 과거 패턴을 2026년 확정 정보처럼 쓰지 않는다.
- 후쿠오카성은 필수 방문지다. 천수대 출입 제한을 후쿠오카성 전체 폐쇄로 표현하지 않는다.
- teamLab, Momochi, Fukuoka Tower, Kushida Shrine을 다시 추천하거나 제외 목록으로 노출하지 않는다.
- 히타는 교통편과 대체편을 구체화하고, 유동적인 시내 일정은 분 단위로 과도하게 고정하지 않는다.
- 쇼핑 정보에는 가능하면 구매 품목, 예상 가격, 우선순위, 재고 성격, 가격 비교 기준과 링크를 유지한다.
- 숙박, 항공, 현지 공용비, 개인 쇼핑 비용을 서로 혼합하지 않는다.

## UI와 구현 원칙

- 이 사이트는 여행 중 스마트폰에서 사용하는 개인용 대시보드다. iPhone과 Galaxy 화면을 최우선으로 검증한다.
- 정보가 많을 때 삭제로 해결하지 않는다. 카드, 탭, 접기, 가로 스크롤 등 모바일에 맞는 표현을 사용한다.
- 현재의 dark UI 방향을 유지한다. 명확한 요구 없이 밝은 관광청 스타일이나 장식 위주의 디자인으로 바꾸지 않는다.
- 중요한 정보도 항상 큰 영역을 차지하게 두기보다 필요할 때 펼쳐보는 상호작용을 고려한다.
- 일정 항목을 추가하거나 이동할 때 DOM 계층을 확인한다. 문자열 위치에 의존한 삽입은 피한다.
- 터치 영역, 가로 넘침, 날짜 탭, 내비게이션, 표 또는 카드 전환, 타임라인 정렬을 모바일 너비에서 확인한다.
- 향후 구조를 정리할 때 여행 데이터와 표시 컴포넌트를 분리해 콘텐츠 회귀 위험을 낮춘다.

## 변경과 검증

- 요구가 충분히 명확하면 설명만 남기지 말고 실제 코드를 수정하고 동작을 검증한다.
- 변경 범위에 맞춰 lint와 build를 실행한다. 실행하지 못한 검증은 결과 보고에 명시한다.
- UI 변경 후 데스크톱과 모바일 화면을 확인한다. 콘텐츠 추가 전후의 정보 보존도 함께 비교한다.
- 버전은 저장소의 현재 버전을 기준으로 Semantic Versioning을 적용한다.
  - 정보 수정, 데이터 추가, 버그 수정: patch
  - 새 UI 또는 의미 있는 기능 추가: minor
  - 대규모 구조 또는 디자인 변경: major
- 사용자 요청 없이 커밋하거나 배포하지 않는다.

## 결과 보고

- 바꾼 파일과 사용자에게 보이는 결과를 먼저 설명한다.
- 확인한 명령과 결과를 간단히 남긴다.
- 미확정 정보, 남은 위험, 출발 직전 다시 확인할 항목을 숨기지 않는다.
- UI 문제를 발견했다면 원인 설명에 그치지 않고 요청 범위 안에서 수정 결과를 제공한다.
